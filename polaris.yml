version: "1"
serverUrl: https://cloudbees.polaris.synopsys.com
install:
  coverity:
    version: default
project:
  name: vulnerable java app
  branch: master
  revision:
    name: java_app
    date: "2025-07-15T00:00:00Z"
capture:
 build:
     cleanCommands:
       - shell: [mvn, -B, -f, pom.xml, clean]
     buildCommands:
       - shell: [mvn, -B, -f, pom.xml, install]
 fileSystem:
    java:
      files:
        - directory: ./src/main/java/org/hdivsamples/bean
        
